NDRM-II：扔掉计数器——一种抗重放、无随机源的轻量级认证新思路

作者：fang-ghost

系列定位：NDRM 系列技术演进 · 第二作

首发时间：2026-07-23

关联文档：

- NDRM-I：《非线性动态响应机制》（2026-07-23）
- NDRM-III：《NDRM-v2：高分支非线性动态响应机制》（即将发表）

引言：从“查表”到“去依赖”的演进

在 NDRM（非线性动态响应机制）的第一代设计中，我们通过“预派生候选池 + 动态查表”模式，初步解决了资源受限硬件令牌的侧信道防御问题。然而，初代方案仍保留了两个传统依赖：单调计数器（Counter）与外部随机源（Challenge）。

在下沉工控场景的长期实践中，这两个依赖暴露出明显的工程短板：设备频繁断电导致 Counter 同步失败，低成本 MCU 缺乏真随机数发生器（TRNG）导致熵源不可靠。更为关键的是，任何对外部输入的依赖，本质上都是攻击面。

为此，本文提出 NDRM 的第二代改进构造。我们在保持候选池查表优势的前提下，彻底废除了 Counter，弱化了随机源依赖，转而构建一种纯状态驱动、无外部刺激依赖的认证新范式。

核心思想：以  Y_{prev}  为唯一导航

传统认证方案像是一个需要不断看手表（Counter）和抽签（Nonce）才能决定下一步的行人。而 NDRM-II 的逻辑截然不同：我们引入前次响应值  Y_{prev}  作为系统唯一的动态熵源，实现完全的状态自治。

索引计算简化为：

I = \text{Hash}(Challenge \parallel Y_{prev})

这一改动带来了三个颠覆性优势：

1. 天然抗重放：由于  Y_{prev}  每次交互后都会不可逆地更新，重放旧 Challenge 将导致索引  I  完全错位，响应自动失效。
2. 零同步负担：设备不再维护可能与验证端偏离的 Counter。无论经历断网、重启或丢包，只要初始状态一致，双方状态机即可通过交互自动对齐。
3. 强隐蔽性：外界观测不到线性计数，也无需传输随机数。所有动态性均隐藏在  Y_{prev}  的非线性更新之中。

弱随机源容忍：从“抽签”到“迷宫”

基于状态驱动思想，Challenge 不再需要具备密码学强度的随机性。在极端情况下，Challenge 可以是一个固定指令字（如 
"0x01"）。

为进一步抬升攻击者的数学分析门槛，我们引入多重单向函数概念。利用“复生根（Composite Roots）”技术，派生多个独立的单向函数路径：

I = \text{HMAC}(K_1, Challenge) \oplus \text{HMAC}(K_2, Y_{prev})

这种构造构成了算法层面的“路径混淆”：

- 攻击者无法通过输入输出关系建立稳定映射，因为  Y_{prev}  持续演化。
- 即使单一函数特性被分析，异或操作与多路耦合仍能维持整体黑盒特性。
- 差分分析、线性逼近等传统手段在此结构下难以找到稳定特征。

承上启下：从工程优化到数学硬化

NDRM-II 的提出，标志着该系列机制从“工程优化”（初代的侧信道防御）迈向了“算法硬化”（状态机与路径混淆）。我们证明了：系统的安全性不应建立在“敌人猜不中随机数”之上，而应建立在“即使敌人完全掌握输入，他也算不出下一个状态”之上。

本作为后续研究奠定了理论基础。在此基础上，我们将进一步引入路径爆炸（Path Explosion）与非线性折叠（Nonlinear Folding）技术，构建具备指数级复杂度的第三代构造——NDRM-v2，以应对更高级别的数学攻击威胁。

结语

NDRM-II 通过废除 Counter、弱化随机源依赖及引入多重单向函数，解决了初代方案的工程落地痛点，并显著提升了算法层面的抗分析能力。这为资源受限、环境恶劣、通信不可靠的工控物联网场景，提供了一个具备高鲁棒性的认证基线。

© 2026 fang-ghost. 保留所有权利。

引用格式：fang-ghost, "NDRM-II：扔掉计数器——一种抗重放、无随机源的轻量级认证新思路", GitHub Repository: [Ghoststep-concept], (2026).

系列声明：本文系 NDRM 系列技术的第二作，系作者在初代 NDRM 基础上的独立演进成果。未经许可，不得用于商业用途。
