补充：高强度对抗下的纵深防御

除了上述基础机制外，针对潜在的拒绝服务（DoS）攻击及侧信道分析（SCA）威胁，本设计引入了以下三层加固
机制，以确保即便在极端对抗环境下，系统仍能维持物理信任锚的完整性。

1. 恒定功耗掩码（Power Balancing）

为抵御基于示波器的差分功耗分析（DPA），系统实现了指令级的功耗均衡。

- 逻辑一致性：无论处理真凭据还是假凭据，CPU 执行的操作码序列、访存周期及外设调度完全一致。
- 波形平坦化：通过引入空操作周期（NOP Padding）及后台低功耗任务调度，将有效运算的功耗峰值填平，
使外部监测设备无法从电流波动中分辨出有效计算与冗余计算的差异。此举
使得依赖统计学的侧信道破解手段失效，即便使用高精度示波器亦无法提取密钥信息。

2. 速率限制与递增惩罚（Rate Limiting）

为防止恶意攻击者通过海量无效指令造成系统阻塞或服务降级，系统内置了流量整形机制。

- 滑动窗口限制：限制单位时间内的指令处理频次（例如 1 秒最多处理 5 次），超出阈值的请求将被静默丢弃，
不占用 CPU 算力进行密码学运算。
- 递增惩罚机制：对于连续鉴权失败的会话，系统将按指数级延长响应间隔。这不仅遏制了高频爆破尝试，同时也
进一步模糊了有效指令与无效指令在宏观时间轴上的统计特征。

3. 中断优先级隔离（Interrupt Isolation）

为确保物理否决权的绝对权威性，系统重构了中断向量表的优先级。

- 最高优先级保留：物理开关检测及看门狗定时器（Watchdog）被设定为不可屏蔽中断（NMI）或最高优先级中断。
- 抗阻塞能力：即便 USB 通讯层被恶意指令洪流淹没，或 MCU 处于高负载运算状态，一旦物理开关触发“维修态”
或“断电”指令，系统将立即抢占 CPU 控制权，强制执行清零与停机
操作，确保攻击无法拖延物理断连。

4. 可选物理熔断机制（Physical Tamper Mesh）

针对面临极高物理风险的应用场景，本设计预留了低成本硬件熔断接口，作为最后一道不可逆的防御手段。

- PCB 防护网：在 PCB 表面布置高精度走线网格（Tamper Mesh），覆盖关键芯片（MCU/EEPROM）。一旦攻击
者尝试剥离芯片或钻孔探测，网格走线断裂。
- 光敏自毁：在芯片 Die 上方开设透光孔并布置光敏元件。若外壳被撬开，环境光射入，触发中断。
- 熔断响应：一旦检测到上述物理入侵信号，中断服务程序将立即唤醒，以最高优先级执行：1) 强制擦除 EEPROM 中
的根种子；2) 向关键 GPIO 输出高电平烧毁前置驱动电路（需外挂熔断电阻）；3) 进入死循环并锁定调试端口。
- 成本说明：此功能涉及额外的 PCB 设计与元件成本，属于可选增强模块。对于常规 30 元级低成本方案，依靠逻辑
层的“维修态断电”与“种子零化”已能提供充分的安全性。
