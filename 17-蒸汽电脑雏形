# 17-蒸汽电脑雏形：物理硬盘与无接触通讯

**作者**：fang-ghost
**日期**：2026-07-23
**关联构想**：GhostFlow Mini、NDRM、主动惯性场
**核心概念**：物理硬盘（ASF）、红外频闪编码、离心增强

## 0. 摘要
本文阐述了 GhostFlow 架构向“蒸汽电脑”（Steam-Punk Computer）演进的两个核心技术突破：
1.  **物理硬盘（ASF）**：将程序逻辑固化为微流控芯片的拓扑结构，实现“换卡带即换程序”的专用计算范式。
2.  **红外频闪通讯**：在离心机高速旋转场景下，利用同步红外光脉冲实现旋转端与固定端的无接触双向信号传输，解决了物理计算核心的观测难题。
这两项技术使得构建无电、抗 EMP、确定性执行的物理计算机成为可能。

## 1. 物理硬盘：流道即代码（ASF）

### 1.1 概念定义
在电子计算机中，程序以数据形式存储在磁介质或闪存中。在 GhostFlow 体系中，程序被定义为**一套不可变的微流道拓扑结构与预置工质分布**。
*   **存储介质**：微流控芯片（Fluidic Chip）。
*   **数据表示**：流道的连通性、特斯拉阀的排布方向、预充染液的浓度梯度。
*   **读写特性**：只读（ROM）。程序在制造时刻被物理固化，运行时无法更改，杜绝了软件层面的篡改风险。

### 1.2 ASF（Application-Specific Fluidics）
借鉴电子 ASIC（专用集成电路）概念，提出 **ASF（专用流体芯片）**。
*   **实例**：一块刻有 NDRM 算法流道的芯片，即为一个“NDRM 物理硬盘”。
*   **模块化**：设计标准化插槽。需执行不同逻辑时，停机更换芯片即可。例如：
    *   **Chip-A**：身份验证逻辑（NDRM）。
    *   **Chip-B**：物理计数逻辑。
    *   **Chip-C**：环境监测逻辑。
*   **优势**：物理隔离，绝无病毒；确定性执行，无缓存无延迟；抗电磁干扰，数据永固（除非物理破坏）。

## 2. 红外频闪通讯：旋转下的无接触总线

### 2.1 痛点：离心场的信号引出
为实现高速响应与抗干扰，GhostFlow 引入离心机产生主动惯性场。然而，旋转部件与固定基座之间的电信号传输需使用滑环，易产生电火花、磨损及电磁泄漏，违背无电与安全初衷。

### 2.2 解决方案：光电同步编码
利用红外光穿透透明腔壁，建立旋转端与固定端的非接触式通信链路。
*   **固定端（发射/接收）**：包含红外发射管与光敏接收器，固定于外部环境。
*   **旋转端（中继）**：包含光敏接收器与红外反馈 LED，随离心机旋转。
*   **供电（无源化）**：旋转端电路不依赖电池，通过**旋转变压器（感应取电）**从固定端耦合获取微量交流电，仅用于驱动 LED 瞬时闪烁。
*   **通讯协议（频闪编码）**：
    1.  **时钟同步**：固定端以离心机转动频率为基准时钟（如 1 圈 = 1 bit）。
    2.  **询问脉冲**：在每圈的特定相位（如 0° 位置），固定端发射一束窄红外脉冲（Query）。
    3.  **物理响应**：旋转端的光敏管收到 Query，根据当前物理状态（如：液柱阻断光路 = 逻辑 '0'；光路通畅 = 逻辑 '1'），控制反馈 LED 在极短时间窗内点亮或熄灭。
    4.  **数据采样**：固定端在预定时间窗内采集旋转端 LED 的反馈光强，解析出 '0' 或 '1'。
*   **优势**：全光学链路，无电磁干扰；无物理接触，无磨损无火花；仅利用物理状态的瞬间光学特性，不干扰核心流体逻辑。

## 3. 蒸汽电脑架构展望

结合物理硬盘与红外通讯，GhostFlow 架构具备构建专用物理计算机（蒸汽电脑）的潜力：
*   **CPU**：流体逻辑阵列（射流元件、特斯拉阀）。
*   **RAM**：毛细液柱标尺（动态状态，断电丢失）。
*   **HDD**：物理硬盘（ASF 芯片，持久化程序）。
*   **Input**：机械打孔卡、液压 Challenge 脉冲、物理开关。
*   **Output**：光路通断指示、双金属片机械动作、打印头压痕。
*   **Bus**：红外频闪通讯链路。

## 4. 结论

“物理硬盘”确立了蒸汽电脑的**存储范式**，使其脱离了“单一功能设备”的局限；“红外频闪通讯”解决了高速旋转下的**观测难题**，打通了物理计算与外部世界的接口。
两者结合，标志着 GhostFlow 从一种安全令牌，进化为一种具备完整 I/O 能力的**物理计算平台**。尽管其速度远低于电子计算机，但在抗毁性、确定性与物理安全性方面，具备不可替代的价值。

---
*© 2026 fang-ghost. 程序刻于流道，信号生于光影。*
